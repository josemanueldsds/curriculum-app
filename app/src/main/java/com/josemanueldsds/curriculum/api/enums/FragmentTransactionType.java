package com.josemanueldsds.curriculum.api.enums;

import java.io.Serializable;

/**
 * Fragment Transactions enum type
 * Specifies the different transaction options,
 * for now they are only "add" and "replace"
 *
 * @author Jose Manuel De Sousa
 * @version 0.1.0
 */
public enum FragmentTransactionType implements Serializable {
    ADD, REPLACE,
}
